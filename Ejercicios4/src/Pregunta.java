public class Pregunta {
   /* Cuál es la diferencia entre sobrecargar (overload) y sobreescribir (override) ?
    Dar un ejemplo de sobrecargar y un ejemplo de sobreescribir.


     La diferencia entre sobrecargar y sobreescribir es que sobrecargar es
     crear un metodo que se llama igual a otro de la clase en la que estamos trabajando
     pero con distintos atributos y que funciona diferente.

     Por el otro lado sobreescribir es crear el mismo metodo en otra clase con los mismos atributos pero
     con contenido diferente dentro. Lo que hace este metodo es utilizar el de la clase original pero
     con los datos escritos encima para esta clase en especifico.
     */



}
